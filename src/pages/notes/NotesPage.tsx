import React from "react";
import NotesList from "../../components/NotesList/NotesList";

const NotesPage = () => {
    return (
        <main>
            <NotesList />
        </main>
    );
};

export default NotesPage;
